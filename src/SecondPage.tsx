import * as React from "react";
import {FunctionComponent} from "react";
import {Typography} from "@material-ui/core";

export const SecondPage: FunctionComponent = (props) => {
    return (
        <Typography variant="h2">
            Second page
        </Typography>
    )
}
